class Item {
    constructor(id, name, price) {
        this.id = id //This is a firebase assigned id;
        this.name = name; //name of item
        this.price = price; //name of price
    }
}

export default Item;
